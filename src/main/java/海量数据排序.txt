1T数据，用16g机器进行排序。

1.拆分文件。拆成10个100g的小文件。每个文件单独排序，得到10个排好序的小文件。
2.每个文件读1g的数据到内存进行归并。读缓存占用10g.
3.将10个文件从头开始读，每读1个数便同当前读到的10个数进行比较【这里可以用小根堆，使排序复杂度减少到logN 】，最小的放入写缓存，最小数所在文件的读指针向后移动。
4.重复上一步。
5.若写缓存满，则将写缓存内的数据写入文件，并清空写缓存。
6.若某一路文件的读指针读到读缓存末尾，则继续加载新一组缓存数据。
7.等所有文件读完后，便写成一个排好序的1T的文件。